syntax = "proto3";

option csharp_namespace = "Proto";

package AudioGRPC;

service AudioTransfer {
    rpc AudioSend(AudioSendRequest) returns (AudioSendReply) {}
}

//TODO: Make correct request and reply messages
message AudioSendRequest {
    string username = 1;
    string pass = 2;
}

message AudioSendReply {
    bool MockLoginSuccess = 1;
}
